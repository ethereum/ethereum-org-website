import fs from "fs"

export const getFilePath = () => {
  const filePaths = []

  // fs.readdir("public/_static", (error, files) => {
  //   if (error) console.log(error)

  //   files.forEach((file) => filePaths.push(file))
  // })
}
