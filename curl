html=$(curl -s https://ethereum.org); echo "🚀 Title: $(echo "$html" | tr '\n' ' ' | sed -n 's/.*<title>\([^<]*\)<\/title>.*/\1/p') — 📝 Description: $(echo "$html" | tr '\n' ' ' | sed -n 's/.*<meta name=\"description\" content=\"\([^\"]*\)\".*/\1/p') — 🔢 'Ethereum' count: $(echo "$html" | grep -o -i 'Ethereum' | wc -l) — 🌐 Status: $(echo "$html" | grep -q 'ethereum' && echo 'alive' || echo 'down')"
