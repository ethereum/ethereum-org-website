# Gemini Feedback: Morphological Variants (Round 4)

Top 20 terms requiring multiple glossary entries for different forms.

1. **stake** -> stake(n), stake(v), staking, staked(adj), staker, unstake, restake, proof-of-stake
2. **sign** -> sign(v), signature(n), signed(adj), signer, multisig, co-sign
3. **validate** -> validate(v), validation(n), validator, valid(adj), validity, invalidated
4. **bridge** -> bridge(n), bridge(v), bridging, bridged, cross-bridge
5. **mint** -> mint(v), minting, minted(adj), minter, premint
6. **burn** -> burn(v), burning, burnt/burned(adj), burn(n), burn rate
7. **swap** -> swap(n), swap(v), swapping, swapper
8. **fork** -> fork(n), fork(v), forking, hard fork, soft fork
9. **vote** -> vote(n), vote(v), voting, voter, veto
10. **hash** -> hash(n), hash(v), hashing, hashed(adj), hash rate
11. **sync** -> sync(v), synchronization(n), syncing, out-of-sync(adj)
12. **attest** -> attest(v), attestation(n), attester, attested(adj)
13. **propose** -> propose(v), proposal(n), proposer, proposed(adj)
14. **verify** -> verify(v), verification(n), verifier, verified(adj), verifiable
15. **slash** -> slash(v), slashing(n/gerund), slashed(adj)
16. **build** -> build(v), builder, building, block building
17. **withdraw** -> withdraw(v), withdrawal(n), withdrawing, withdrawn(adj)
18. **finalize** -> finalize(v), finality(n), finalized(adj)
19. **chain** -> chain(n), on-chain(adj), off-chain(adj), sidechain, multichain
20. **claim** -> claim(n), claim(v), claiming, claimable(adj)
